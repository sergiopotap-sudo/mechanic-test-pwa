const CACHE='mechanic-test-offline-v4';
const CORE=['./','./index.html','./styles.css?v=4','./app.js?v=4','./manifest.webmanifest?v=4','./assets/icon.svg','./data/parts.json?v=4'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(CORE)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const req=event.request;const url=new URL(req.url);const shell=url.pathname.endsWith('/index.html')||url.pathname.endsWith('/mechanic-test-pwa/');if(shell){event.respondWith(fetch(req).then(r=>{const c=r.clone();caches.open(CACHE).then(x=>x.put(req,c));return r;}).catch(()=>caches.match(req).then(h=>h||caches.match('./index.html'))));return;}event.respondWith(caches.match(req).then(hit=>hit||fetch(req).then(r=>{const c=r.clone();caches.open(CACHE).then(x=>x.put(req,c));return r;})));});
self.addEventListener('message',event=>{if(event.data?.type==='CACHE_ALL'){event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(CORE)).then(()=>event.source?.postMessage({type:'CACHE_ALL_DONE',cached:CORE.length,required:CORE.length})));}if(event.data?.type==='GET_STATUS'){event.waitUntil(caches.open(CACHE).then(async cache=>{let cached=0;for(const u of CORE){if(await cache.match(u))cached++;}event.source?.postMessage({type:'CACHE_STATUS',cached,required:CORE.length,complete:cached===CORE.length});}));}});